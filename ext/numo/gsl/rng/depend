COGEN=erb
GENSRC=gsl_rng.c
ERBSRC=gen/gsl_rng.erb.c
GENDEPS=$(ERBSRC)

$(GENSRC) : $(GENDEPS)
	$(COGEN) $(ERBSRC) > $@

doc : $(GENSRC)
	yard doc $(GENSRC) types/*.c

clean: cleansrc cleandoc
cleansrc:
	-$(Q)$(RM) $(GENSRC)
cleandoc:
	-$(Q)$(RM_RF) doc .yardoc
